%body.default-body
.member-registration

  = render "devise/registrations/rg-header"
  %main
    .member-information
      %h2.l-head 
        会員登録完了
      .default-confirmation
        .form-fields
          %p ありがとうございます。会員登録が完了しました。
          %button.red-btn{type: "submit"} 
            = link_to "メルカリを始める", products_path
          %button.red-btn{type: "submit"} メルカリを始める
          %button.red-btn
            = link_to "メルカリを始める", mypages_path, class: 'btn'
  .footer__ad
    = image_tag("footer.png", width: 700)

  = render "devise/registrations/rg-footer"