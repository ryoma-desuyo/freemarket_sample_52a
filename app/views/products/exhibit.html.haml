- if user_signed_in? && @product.seller_id == current_user.id
  %head
  = render 'shared/mypage-header'
  - breadcrumb :exhibit
  = render "layouts/breadcrumbs"
  .details-back
    = render "products/exhibit"

  = render "shared/footer"
- else
  = render "products/details" 